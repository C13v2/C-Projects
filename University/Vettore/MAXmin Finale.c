#include <stdio.h>
#include <stdlib.h>
#include <time.h>


void funzione (int vettore[], int dim , int *max , int *min);
int main()
{
	int i;
	int a,b,M,m;
	int vett[i];
	printf ("inserisci due numeri");
	scanf ("%d %d" , &a, &b);
	 
	funzione(int max,int min, int *max, int *min);
	printf ( "max: %d , min %d", max, min);
	
	return 0 ;

}

void funzione (int vettore[], int dim , int *max , int *min);
{
	int i;
	for (i=0; i<N;i ++);
	{
		if (vett[i]>*max);
		{
			*max=vett[i];
		}
		if (vett[i]<*min);
		{
			*min=vett[i];
		}
		
	}	
}

